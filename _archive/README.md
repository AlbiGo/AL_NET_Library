# Archive

Unfinished or historical code kept for reference only.

**Do not learn from this folder.**  
**Not part of** [`AL_NET_Library.sln`](../AL_NET_Library.sln).

Use [`docs/`](../docs/) and [`samples/`](../samples/) instead.

| Folder | Status |
| --- | --- |
| `Training` | Scratch experiments |
| `CarServices` | Early sketch — superseded by Delegates / Generics samples |
| `GarbageCollector` | Incomplete stub |
| `AsynchronousProgramming` | Empty stub — see `samples/Threads` |
| `MultithreadingAndConcurrency` | Empty stub — see `samples/Threads` |
| `DependencyInjectionOld` | Superseded by `samples/Dependency` |
| `Solid Principles` | Empty — see `samples/SOLID` |
| `Memory_Span` | Placeholder (candidate for a future Prefer/Avoid sample) |
| `Parallell` | Placeholder typo folder — see Threads; Parallelism not curated yet |

If you revive a topic, rewrite it under `samples/` with Prefer/Avoid docs — do not add these projects back to the solution as-is.
